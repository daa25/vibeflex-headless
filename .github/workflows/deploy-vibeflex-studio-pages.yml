name: Deploy VibeFlex Studio Pages
on:
  push:
    branches: [main]
    paths:
      - 'vibeflex-studio-pages/**'
      - '.github/workflows/deploy-vibeflex-studio-pages.yml'
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: vibeflex-studio-pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: vibeflex-studio-pages
      - id: deployment
        uses: actions/deploy-pages@v4
